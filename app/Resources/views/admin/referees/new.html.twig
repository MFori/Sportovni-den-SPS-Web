{% extends 'base.html.twig' %}
{% block content %}
    {% if app.request.attributes.get('_route') == 'referees_new' %}
        <h1>Nový rozhodčí</h1>
    {% else %}
        <h1>{{ user.username }}</h1>
    {% endif %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}

{% block includes %}
    <script src="{{ asset('Resources/js/page/referees.min.js') }}"></script>
{% endblock %}