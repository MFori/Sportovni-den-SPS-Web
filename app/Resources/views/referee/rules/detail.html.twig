{% extends 'base.html.twig' %}
{% block content %}
    <h1>{{ rules.sport.name }}</h1>
    <form method="post">
        <div class="form-group">
            <textarea class="form-control" rows="3" name="rules_text">{{ rules.text }}</textarea>
        </div>
        <div class="form-group">
            <input class="form-control" type="submit" value="Uložit">
        </div>
    </form>
    <a href="{{ path('rules') }}"><- Všechny sporty</a>
{% endblock %}